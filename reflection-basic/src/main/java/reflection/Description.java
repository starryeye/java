package reflection;

public interface Description {
    /**
     * reflection..
     *      클래스가 제공하는 다양한 정보를 런타임에 "동적"으로 분석하고 사용하는 기능을 리플랙션(reflection) 이라 한다.
     *      제공되는 기능
     *          런타임에 클래스, 메서드, 필드, 애너테이션 등에 대한 정보를 얻을 수 있다.
     *          런타임에 새로운 객체 인스턴스를 생성하고 메서드를 호출할 수 있다.
     *          런타임에 객체의 필드값을 읽고 쓸 수 있다.
     *
     * 참고.
     * 여기서의 "동적"이다 라는 의미는..         참고
     */
}
