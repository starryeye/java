package io.sub4_files_and_path;

public interface FilesAndPath {

    /**
     * Java 가 제공하는 File, Files, Path 로 Java 프로세스와 파일간 io 를 다루어본다. (폴더와 파일을 CURD)
     *      basic stream 인 FileInputStream/FileOutputStream 으로 직접 io 를 다루거나
     *      filter stream 인 FileReader/FileWriter + BufferedReader/BufferedWriter 으로 io 를 다루기보다
     *      Files, Path 로 다루는 것이 더 편리하고 성능상 최적화 고려가 되어있다.
     *
     * File
     *      Java 1.0, 옛날 방식
     * Files, Path, Paths
     *      Java 1.7, 요즘 방식
     */
}
