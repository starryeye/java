package dev.practice.basic.sub2_v1;

public interface Description {

    /**
     * 가장 기본적인 TCP/IP 네트워크 통신의 client - server 이다.
     *
     * Socket::getInputStream(), Socket::getOutputStream() 을 통해 통신을 할 수 있는 basic stream 을 얻을 수 있다.
     *      filter stream 으로 감싸서 인코딩/디코딩 할 필요없이 Java data type 으로 편리하게 통신하도록 하였다.
     */
}
