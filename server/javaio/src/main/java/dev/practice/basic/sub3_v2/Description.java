package dev.practice.basic.sub3_v2;

public interface Description {

    /**
     * V1 과 비교하여.. V2 는..
     *      try with resources 로 자원정리를 함
     *      하나의 server 가 여러 client 를 받을 수 있다.(session)
     *      client 입장에서 한번만 메시지를 주고받는게 아니라 여러번 원하는만큼 주고받을 수 있다.
     *      client 가 "exit" 메시지를 보내면 연결이 종료된다.
     */
}
