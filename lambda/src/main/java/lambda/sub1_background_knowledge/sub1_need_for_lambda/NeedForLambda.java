package lambda.sub1_background_knowledge.sub1_need_for_lambda;

public interface NeedForLambda {

    /**
     * sub2 를 보면..
     * 특정 코드 영역을 외부에서 전달하기 위해 behavior parameterization 을 적용하는데..
     * After1, After2 방법(Java 8 이전의 방법)으로 하려면.. 클래스 및 인스턴스를 직접 생성해야해서.. Boilerplate 코드가 많아서 별로다..
     *
     * 간단하게 sub1 처럼 코드 영역만 전달하는 방법이 필요하다.
     * -> 람다
     */
}
